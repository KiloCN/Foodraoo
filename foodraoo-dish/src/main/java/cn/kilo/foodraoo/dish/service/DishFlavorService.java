package cn.kilo.foodraoo.dish.service;

import cn.kilo.foodraoo.feign.pojo.DishFlavor;
import com.baomidou.mybatisplus.extension.service.IService;

/**
 * The DishFlavorService interface is responsible for providing service methods related to DishFlavor objects.
 * It extends the IService interface, which provides the basic CRUD operations.
 *
 * @author kilo
 * @version 0.0.1-SNAPSHOT
 */
public interface DishFlavorService extends IService<DishFlavor> {
}
